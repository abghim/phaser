# phaser
Painless gradients for your terminal.

## Install
Available on cargo:
```
cargo install phaser-tty
```

The macho way:
```
git clone https://github.com/abghim/phaser && cd phaser && cargo build --release && cp target/release/phaser ~/.local/bin/
```
